experiment:
  name: g2_defense_smoke
  mode: smoke
  models: [preact_resnet18]
  defenses: [standard, fgsm_at, pgd_at, fixed_mixed_at]
  attacks: [fgsm, pgd20, apgd_ce, apgd_dlr]
dataset:
  name: fake_cifar10
  mode: smoke
  train_subset_size: 128
  val_subset_size: 32
  eval_subset_size: 32
training:
  epochs: 1
  max_train_batches: 2
  max_eval_batches: 1

