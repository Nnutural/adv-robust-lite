# C9 Patch Report

## Changed
- Added `mode={real,smoke}` to train, clean eval, attack eval, AA-Lite, AutoAttack subset, and diagnostics scripts.
- Dataset layer now rejects `dataset_name=fake_cifar10` in `mode=real`.
- New outputs write under `results/{mode}/raw/...`; JSON/metrics include `mode` and `dataset_name`.
- Aggregation defaults to `results/real/raw` and rejects smoke/fake results unless `--allow-smoke` is set.
- Migrated existing development fake outputs from `results/raw/` to `results/smoke/raw/`.
- Added smoke/real experiment config files and `tests/test_c9_mode_isolation.py`.

## Tests
- `python -m pytest tests -q`: `13 passed, 18 skipped`.
- Fake smoke passed with `--mode smoke`: train -> FGSM -> PGD-2 -> AA-Lite-2 -> aggregate -> figures.
- Default real aggregation on empty `results/real/raw` succeeded; smoke aggregation without `--allow-smoke` failed as intended.

## Sanity Gate
- Satisfies plan-v2 C9: fake/smoke results are physically separated and real aggregation refuses smoke/fake metadata.
- Smoke results remain engineering-only and are not valid paper metrics.
