# C5 Patch Report

- Added named eval subset config and deterministic `eval_subset_id`.
- Attack and AA-Lite JSON now carry `eval_subset_id`.
- AA-Lite marks `gap_over_error=subset_id_mismatch` if child attacks disagree on subset IDs.
- Aggregation preserves `eval_subset_id` and `gap_over_error`.
- Added `tests/test_c5_subset_id.py`.
- Tests: `python -m pytest tests -q` passed: `9 passed, 14 skipped`.
- Fake smoke train/evaluate_attack/run_aalite/aggregate/make_figures passed.
- plan-v2 C5 sanity gate is satisfied for metadata and same-subset AA-Lite; real 2k subset use is available via `--eval-subset aalite_2k`.
