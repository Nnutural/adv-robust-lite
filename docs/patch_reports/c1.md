# C1 Patch Report

- Added Trap-A/Trap-B wrappers: `LogitScaleWrapper` and `InputRandomizationWrapper`.
- Added PGD EOT gradient path and propagated `eot_samples` / demo metadata through runner and AA-Lite.
- Added trap configs and G3 trap experiment stubs.
- Added `tests/test_c1_traps.py`.
- Tests: default `python -m pytest tests -q` passed: `6 passed, 10 skipped`.
- Torch pytest in `adversarial_attack` could not run because pytest is not installed there.
- Fake smoke train/evaluate_attack/run_aalite/aggregate/make_figures passed using the old pre-C9 results path.
- APGD-CE/APGD-DLR skipped because `torchattacks` is not installed.
- plan-v2 C1 sanity gate is functionally implemented, but `Gap_over(Trap-A)>0.20` still needs a dependency-enabled APGD-DLR run to verify numerically.
