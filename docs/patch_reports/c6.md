# C6 Patch Report

- APGD-CE/APGD-DLR defaults are now 50 steps in config and factory defaults.
- Added Square attack config with `n_queries=2000` and subset-size guidance.
- Added Square factory branch with skipped status when `torchattacks` is unavailable.
- Added `tests/test_c6_apgd_steps_square.py`.
- Tests: `python -m pytest tests -q` passed: `9 passed, 17 skipped`.
- Fake smoke train/evaluate_attack/run_aalite/aggregate/make_figures passed.
- Extra Square smoke returned `status=skipped` due to missing `torchattacks`, as required.
- plan-v2 C6 sanity gate is satisfied for defaults and missing-dependency behavior.
