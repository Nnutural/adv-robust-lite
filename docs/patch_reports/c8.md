# C8 Patch Report

## Changed
- Added `max_wall_seconds`, `batch_size`, `session_id`, and `experiment_group` to `TrainConfig`.
- Trainer now records `gpu_name`, `batch_size`, `amp`, `session_id`, `max_wall_seconds`, and `wall_clock_stopped`.
- Added `--max-wall-seconds` and `--experiment-group` to `scripts/train.py`.
- Aggregation preserves budget fairness fields and marks inconsistent groups with `equal_budget_invalid`.
- Added `tests/test_c8_budget_columns.py`.

## Tests
- `python -m pytest tests -q`: `12 passed, 17 skipped`.
- Fake smoke passed: train -> FGSM -> PGD-2 -> AA-Lite-2 -> aggregate -> figures.
- APGD remained `skipped` because `torchattacks` is unavailable; no metrics were fabricated.

## Sanity Gate
- Satisfies plan-v2 C8 metadata gate: budget rows include GPU, batch, AMP, session, and wall-clock budget fields.
- Equal-GPU-hour comparisons are now explicitly invalidated when fairness fields disagree.
