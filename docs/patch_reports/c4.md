# C4 Patch Report

- Added `fgsm_rs_attack` and registered `fgsm_rs` in `AttackFactory`.
- FGSM-AT now defaults to FGSM-RS via defense config.
- Added CO monitoring fields: `co_detected`, `co_epoch`, `co_pgd7_history`.
- Added `tests/test_c4_fgsm_rs_co.py`.
- Tests: `python -m pytest tests -q` passed: `7 passed, 13 skipped`.
- Extra FGSM-AT fake smoke confirmed `best_criterion=robust_val`, `train_attack_config.name=fgsm_rs`, and CO fields exist.
- Standard fake smoke train/evaluate_attack/run_aalite/aggregate/make_figures passed.
- plan-v2 C4 sanity gate is satisfied structurally; real CO behavior requires longer real training.
