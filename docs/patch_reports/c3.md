# C3 Patch Report

- Added `train_attack` and `eval_attacks` to `TrainConfig` and metrics.
- `scripts/train.py` now reads defense yaml by default or via `--defense-config`.
- PGD-AT config records PGD-7 training and PGD-20/APGD-50 eval attack names.
- Fixed Mixed-AT keeps its staged mix semantics instead of being overridden by global `train_attack`.
- Added `tests/test_c3_train_eval_split.py`.
- Tests: `python -m pytest tests -q` passed: `7 passed, 12 skipped`.
- Fake smoke train/evaluate_attack/run_aalite/aggregate/make_figures passed.
- plan-v2 C3 sanity gate is satisfied: train PGD steps are recorded as 7 and eval includes PGD-20.
