# C7 Patch Report

## Changed
- Added explicit AA-Lite scope metadata: `r_lite_scope="whitebox"` and separate black-box notes.
- Preserved `r_lite_scope` in aggregated robustness tables.
- Updated README to state that `R_lite` is white-box only and Square is evaluated separately.
- Added `tests/test_c7_r_lite_scope.py`.

## Tests
- `python -m pytest tests -q`: passed in the current smoke environment (`11 passed, 17 skipped` before C8).
- Fake smoke chain passed before C8: train -> FGSM -> PGD-2 -> AA-Lite-2 -> aggregate -> figures.

## Sanity Gate
- Satisfies plan-v2 C7: README and AA-Lite payload explicitly declare white-box-only scope.
- Black-box attacks remain separate from `R_lite`; no Square result is folded into AA-Lite.
