# C2 Patch Report

- Added robust-val best checkpoint support in `Trainer`.
- Standard uses `best_criterion=clean_val`; AT defenses default to `robust_val`.
- `metrics.json` now includes best criterion, metric, value, epoch, clean-best and robust-best fields.
- Added defense config fields for `best_criterion` and `robust_val`.
- Added `tests/test_c2_best_robust_val.py`.
- Tests: default `python -m pytest tests -q` passed: `6 passed, 11 skipped`.
- Fake smoke train/evaluate_attack/run_aalite/aggregate/make_figures passed.
- plan-v2 C2 sanity gate is satisfied structurally; torch-level C2 unit test is skipped in default env because torch is absent.
